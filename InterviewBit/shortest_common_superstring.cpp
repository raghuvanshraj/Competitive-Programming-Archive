#include <bits/stdc++.h>

using namespace std;

string solve(vector<string> strings) {
	
}

int main(int argc, char const *argv[]) {
	int n;
	cin >> n;
	vector<string> strings(n);
	for (int i = 0; i < n; ++i) {
		cin >> strings[i];
	}

	cout << solve(strings);
	
	return 0;
}